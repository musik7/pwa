{
  "name": "pwa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx serve -l 3000",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "playwright": "^1.35.0",
    "serve": "^14.2.0"
  }
}
